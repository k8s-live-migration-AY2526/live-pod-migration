---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: podrestores.lpm.my.domain
spec:
  group: lpm.my.domain
  names:
    kind: PodRestore
    listKind: PodRestoreList
    plural: podrestores
    singular: podrestore
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Referenced PodCheckpointContent
      jsonPath: .spec.podCheckpointContentRef.name
      name: PodCheckpointContentName
      type: string
    - description: Target node for restore
      jsonPath: .spec.targetNode
      name: TargetNode
      type: string
    - description: Current phase of the restore
      jsonPath: .status.phase
      name: Phase
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: PodRestore is the Schema for the podrestores API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PodRestoreSpec defines the desired state of PodRestore.
            properties:
              isDeployment:
                description: |-
                  IsDeployment indicates whether this is a Deployment pod restoration.
                  When true, the controller will delete the original pod and let the Deployment
                  controller recreate it with checkpoint images injected via the webhook.
                type: boolean
              isStatefulSet:
                description: |-
                  IsStatefulSet indicates whether this is a StatefulSet pod restoration.
                  When true, the controller will patch the StatefulSet template and manage
                  pod recreation through the StatefulSet controller.
                type: boolean
              podCheckpointContentRef:
                description: |-
                  PodCheckpointContentRef references the PodCheckpointContent that holds
                  checkpoint artifacts and the podSpecSnapshot used for restore.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              restoredPodName:
                description: RestoredPodName is the name to assign to the restored
                  Pod.
                type: string
              targetNode:
                description: |-
                  TargetNode optionally pins the restored Pod to a specific node.
                  If empty, the scheduler will decide placement.
                type: string
            required:
            - podCheckpointContentRef
            type: object
          status:
            description: PodRestoreStatus defines the observed state of PodRestore.
            properties:
              completionTime:
                description: CompletionTime indicates when restore finished (on terminal
                  states).
                format: date-time
                type: string
              deploymentRestore:
                description: |-
                  DeploymentRestore contains information needed to track the original Deployment pod
                  during migration. This field is only populated for Deployment pods.
                properties:
                  originalPodUID:
                    description: |-
                      OriginalPodUID stores the UID of the original pod to distinguish it
                      from the recreated pod during Deployment migration.
                    type: string
                required:
                - originalPodUID
                type: object
              imageMapping:
                additionalProperties:
                  type: string
                description: ImageMapping maps container name -> prepared image reference
                  used during restore.
                type: object
              message:
                description: Message is a human-readable status or error.
                type: string
              phase:
                description: Phase is the high-level lifecycle state.
                type: string
              restoredPodName:
                description: RestoredPodName is the name of the Pod created by the
                  restore controller.
                type: string
              startTime:
                description: StartTime indicates when restore began.
                format: date-time
                type: string
              statefulSetRestore:
                description: |-
                  StatefulSetRestore contains information needed to restore the original StatefulSet
                  template after migration completion. This field is only populated for StatefulSet pods.
                properties:
                  originalPodUID:
                    description: |-
                      OriginalPodUID stores the UID of the original pod to distinguish it
                      from the recreated pod during StatefulSet migration.
                    type: string
                required:
                - originalPodUID
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
